# Instagram運用管理ツール セットアップ＆操作ガイド

## はじめに

このツールは、Googleスプレッドシート上でInstagram・LINE・議事録を一元管理するシステムです。
すべての操作はスプレッドシートのメニュー「📊 IG管理ツール」からボタンを押すだけで行えます。


---


## 1. 初期セットアップ（初回のみ）

### 1-1. Gemini APIキーの取得

AIによる考察生成・スクショ読み取りに必要です。

1. https://aistudio.google.com/apikey にアクセス
2. Googleアカウントでログイン
3. 「APIキーを作成」をクリック
4. 表示されたキー（AIza...で始まる文字列）をコピー

※ 無料枠で月間十分に利用できます。


### 1-2. スプレッドシートの初期設定

1. スプレッドシートを開く
2. メニュー「📊 IG管理ツール」→「⚙️ 設定」→「🚀 初期セットアップ（初回のみ）」
3. APIキーの入力を求められるので、1-1で取得したキーを貼り付けてOK
4. 自動でデータ更新・グラフ作成・書式設定が行われます

※ メニューが表示されない場合はページをF5で再読み込みしてください。


---


## 2. Instagramデータの入力方法

データの入力方法は3つあります。

### 方法A: スクショから自動読み取り（推奨）

スマホでInstagramのインサイト画面をスクリーンショットし、Googleドライブに入れるだけで自動的にデータを読み取ります。
スクショは投稿ごとにフォルダを分ける必要はなく、まとめて入れてOKです。


#### 投稿データの取得手順

1. スマホでIGアプリを開く
2. プロフィール → インサイト → コンテンツ → 投稿一覧の画面を表示
3. 上から順にスクロールしながらスクリーンショットを2〜3枚撮影
4. 撮影したスクショをGoogleドライブの以下のフォルダに保存
   → 「IGインサイトスクショ」→「投稿」フォルダ
5. スプレッドシートで「📊 IG管理ツール」→「📸 投稿スクショ読み取り」
6. 自動で読み取りが行われ、①投稿管理シートにデータが書き込まれます

※ 処理済みのスクショは自動で「処理済み」フォルダに移動します。


#### アカウントインサイトの取得手順

1. IGアプリ → プロフィール → インサイト → 概要画面を表示
2. 以下の画面を上から順にスクロールしながらスクリーンショット（約4枚）
   - 閲覧数・フォロワー内外の割合
   - オーディエンス（年齢層）
   - プロフィールアクティビティ（プロフアクセス・リンクタップ）
   - フォロワー数・フォロワー増減
3. スクショをGoogleドライブの以下のフォルダに保存
   → 「IGインサイトスクショ」→「アカウント」フォルダ
4. スプレッドシートで「📊 IG管理ツール」→「📸 アカウントスクショ読み取り」
5. 自動で読み取りが行われ、②月次インサイトシートにデータが書き込まれます

※ 読み取れなかった項目がある場合、完了メッセージに警告が表示されます。
   その場合は②シートを確認し、手動で補完してください。


### 方法B: 手動入力

①投稿管理シート・②月次インサイトシートに直接数値を入力することもできます。


### 方法C: スクショ取得の自動化（オプション）

手動スクショが手間な場合、以下の自動化も利用できます。

#### iPhoneの場合（半自動・推奨）

iOSの「ショートカット」アプリを使って、
「背面タップ → スクショ → Drive自動保存」を設定できます。
スクロールは手動ですが、保存が自動化されるため作業時間が短縮されます。

→ 詳細は「ios_shortcut_guide.md」を参照

#### Androidの場合（完全自動）

PCとAndroidを同じWiFiに接続し、Pythonスクリプトを実行するだけで
スクショ撮影→Drive保存まで全自動で行えます。
初回のみUSBケーブルでのセットアップが必要です（5分程度）。

→ 詳細は「android_setup_guide.md」を参照

※ どちらの方式でも、Drive保存後の読み取りはスプシのボタン操作（方法Aと同じ）です。


---


## 3. 月次レポート作成の流れ

毎月のレポート作成は以下の順番で行います。

### ステップ1: データ入力
上記「2. Instagramデータの入力方法」でデータを入力

### ステップ2: 全データ更新
- ③自動集計シートのB3セルで対象月を選択
- 「📊 IG管理ツール」→「🔧 全データ更新」
- ③に集計結果が自動反映、⑤レポート出力も自動構築されます

### ステップ3: AI考察生成
- 「📊 IG管理ツール」→「🤖 AI考察を生成」
- ④考察・コメントシートに4セクション（傾向・良い点・改善点・次月施策）が自動生成
- ⑤レポート出力にもAI考察が反映されます

### ステップ4: 確認・手動修正
- ④の内容を確認し、必要に応じて手動で修正
- ⑤レポート出力シートの内容も確認

### ステップ5: PDF出力
- 「📊 IG管理ツール」→「📄 レポートをPDF出力」
- Driveの「IGレポート」フォルダにPDFが保存されます


### ⑤レポートシートの編集ルール（ロゴ・画像の入れ方）

⑤レポート出力シートは「全データ更新」「AI考察を生成」のたびに自動で作り直されます。
そのため、編集できる箇所とできない箇所があります。

- ❌ セルへの直接入力・文字修正 → 更新時に消えます
  （コメント文を直したい場合は ④考察・コメント シートを修正してください。⑤に自動反映されます）
- ✅ 会社ロゴ・写真などの画像 → メニュー「挿入」→「画像」→「セルの上に画像を挿入」で配置してください
  （画像は更新後もそのまま残ります。一度配置すれば毎月使えます）

※ ⑤シートのA1セルにも同じ内容のメモを付けています（セルにカーソルを合わせると表示されます）。


---


## 4. LINE分析の使い方

### CSVのダウンロード方法

1. LINE公式アカウント管理画面にログイン（ https://manager.line.biz/ ）
2. 「分析」→「統計情報をダウンロード」
3. 以下の2つをダウンロード
   - 「友だち」→「概要」（friend_overview_YYYYMMDD_YYYYMMDD.csv）
   - 「メッセージ通数」（message_overview_YYYYMMDD_YYYYMMDD.csv）

### 取り込み手順

1. ダウンロードしたCSVをGoogleドライブの「LINE_CSV」フォルダに保存
2. スプレッドシートで「📊 IG管理ツール」→「📱 LINE CSVを取り込み」
3. 日次データが自動で月次に集計され、⑦LINE分析シートに書き込まれます

### 取り込まれるデータ

| 項目 | 元データ |
|------|---------|
| 友だち数 | 月末時点の友だち数 |
| ターゲットリーチ | 月末時点のターゲットリーチ数 |
| 新規友だち | 月初〜月末の友だち増加数 |
| 新規ブロック | 月初〜月末のブロック増加数 |
| 自動応答〜API返信 | 各メッセージ種別の月間合計 |

### LINE AI考察
- 「📊 IG管理ツール」→「📱 LINE AI考察を生成」
- ⑦シートのデータに基づいてAIが傾向と改善点を自動生成します

### LINEレポートのPDF出力
- 「📊 IG管理ツール」→「📱 LINEレポートをPDF出力」
- ⑦のデータとAI考察をもとに ⑧LINEレポート シートが自動で作られ、
  そのままPDF化されてDriveの「IGレポート」フォルダに保存されます
- お客様への提出用にお使いください（IGレポートと同様、ロゴ画像の配置も可能です）

※ 開封率・クリック率はメッセージ配信を行っている場合のみCSVに含まれます。


---


## 5. 議事録の使い方

打ち合わせの録音ファイルから、自動で文字起こし＋議事録フォーマットに整理します。

### 手順

1. 打ち合わせを録音（iPhoneのボイスメモ等）
2. 録音ファイル（m4a/mp3/wav）をGoogleドライブの「議事録」→「録音」フォルダに保存
3. スプレッドシートで「📊 IG管理ツール」→「📝 議事録を処理」
4. 録音ファイルの一覧が表示されるので、処理する番号を入力
5. クライアント名・日付を入力
6. 処理開始（バックグラウンドで10〜15分かかります。長い録音はもう少しかかります）
7. 完了するとDriveの「議事録」→「出力」フォルダに議事録が保存されます

※ 初回実行時にGitHub Personal Access Tokenの入力を求められます。
   一度入力すれば以降は不要です。（→ 取得方法は「github_token_guide.md」参照）
※ 出力された議事録はテキスト形式なので、参加者名の追記など自由に編集できます。

### 議事録のフォルダ構成

```
議事録/
├── 録音/       ← ここに録音ファイルを入れる
├── 処理済み/   ← 処理後の録音が自動移動
├── 文字起こし/ ← 生テキスト（バックアップ）
└── 出力/       ← 完成した議事録
```


---


## 6. 新規クライアント追加（フォルダ丸ごと複製）

クライアントが増えたら、手動でコピーせず必ずこの機能を使ってください。
フォルダ一式が複製されるため、クライアント間でスクショやレポートが混ざりません。

1. 「📊 IG管理ツール」→「📋 テンプレートをコピー」
2. クライアント名を入力
3. 現在のプロジェクトフォルダと並びに「IG管理_クライアント名」フォルダが作成されます

作成される中身（全部入りのパッケージです）:

```
IG管理_クライアント名/
├── Instagram管理ツール（データ空のスプレッドシート）
├── セットアップガイド等の資料一式
├── IGレポート/
├── IGインサイトスクショ/（投稿・アカウント）
├── LINE_CSV/
└── 議事録/（録音・処理済み・文字起こし・出力）
```

4. 新しいスプレッドシートを開いて、以下を実行してください
   1. ⚙️ 設定 → 🔑 APIキー設定
   2. ⚙️ 設定 → 📁 フォルダ設定を反映
   3. ⚙️ 設定 → 🚀 初期セットアップ

※ フォルダIDは⑥シートに自動で書き込まれているので、コピー&ペーストは不要です。
※ 議事録を使う場合は、初回実行時にGitHubトークンの入力も必要です。
※ クライアント様にツールをお渡しする場合は、このフォルダごと共有すればそのまま使えます。


---


## 7. Driveフォルダ構成

```
プロジェクトフォルダ/
├── Instagram管理ツール（スプレッドシート本体）
├── セットアップガイド等の資料
├── IGレポート/              ← IG・LINEレポートのPDF出力先
├── IGインサイトスクショ/
│   ├── 投稿/               ← 投稿インサイトのスクショを入れる
│   └── アカウント/          ← アカウントインサイトのスクショを入れる
├── LINE_CSV/               ← LINE管理画面のCSVを入れる
└── 議事録/
    ├── 録音/               ← 録音ファイルを入れる
    ├── 処理済み/           ← 処理後の録音が自動移動
    ├── 文字起こし/         ← 生テキスト（バックアップ）
    └── 出力/               ← 完成した議事録
```


---


## 8. トラブルシューティング

### メニューが表示されない
→ スプレッドシートをF5で再読み込みしてください。

### 「APIキー未設定」と表示される
→ 「⚙️ 設定」→「🔑 Gemini APIキーを設定」からキーを入力してください。

### AI考察が生成されない
→ 「⚙️ 設定」→「🔍 APIキー動作テスト」でAPI接続を確認してください。

### スクショ読み取りが失敗する
→ 画像が暗い、解像度が低い可能性があります。明るい状態で撮り直してください。
→ 対応形式: PNG / JPG / JPEG / WebP

### LINE CSVが読み込まれない
→ 「LINE_CSV」フォルダに直接CSVを配置してください（サブフォルダ内は対象外）。

### ⑤レポートに入れた文字が消えた
→ ⑤は自動再構築されるシートです。文章の修正は④考察・コメントシートで行ってください。
   ロゴ等の画像は「セルの上に画像を挿入」で配置すれば消えません（3章参照）。


---


## 9. メニュー一覧

| メニュー | 機能 |
|---------|------|
| 🔧 全データ更新 | ③の集計を更新＋⑤レポートを自動構築 |
| 🤖 AI考察を生成 | ④にAI考察を生成＋⑤に反映 |
| 📄 レポートをPDF出力 | ⑤をPDF化してDriveに保存 |
| 📸 投稿スクショ読み取り | Drive→AI読み取り→①に書き込み |
| 📸 アカウントスクショ読み取り | Drive→AI読み取り→②に書き込み |
| 📋 テンプレートをコピー | 新規クライアント用フォルダ一式を自動作成 |
| 📱 LINE CSVを取り込み | CSVを月次集計→⑦に書き込み |
| 📱 LINE AI考察を生成 | ⑦のデータからAI考察を生成 |
| 📱 LINEレポートをPDF出力 | ⑧LINEレポートを構築→PDF化してDriveに保存 |
| 📝 議事録を処理 | 録音→文字起こし→議事録整理→Drive保存 |
| ⚙️ 設定 | APIキー設定・テスト・グラフ再作成・初期セットアップ |


---


## 10. 自分のDriveでフォルダを構築する（手動設定する場合）

通常は「📋 テンプレートをコピー」（6章）を使えばこの作業は不要です。
既存のフォルダ構成に接続したい場合のみ、以下の手順で設定してください。

### ステップ1: フォルダ作成

Googleドライブに「7. Driveフォルダ構成」と同じ構成でフォルダを作成します。

### ステップ2: フォルダIDをコピー

各フォルダをブラウザで開くと、URLが以下のようになります:

```
https://drive.google.com/drive/folders/1ABCDefGHijKLmnOPqrsTUvwxYZ
                                       ↑ この部分がフォルダID
```

以下6つのフォルダIDをメモしてください:

1. プロジェクトフォルダ本体
2. IGレポート
3. IGインサイトスクショ > 投稿
4. IGインサイトスクショ > アカウント
5. LINE_CSV
6. 議事録

### ステップ3: スプシに設定

1. ⑥コンセプト設計シートの行30〜35に設定エリアがあります

```
行30: A列「プロジェクトフォルダ」   B列にフォルダIDを貼り付け
行31: A列「IGレポート」            B列にフォルダIDを貼り付け
行32: A列「スクショ（投稿）」       B列にフォルダIDを貼り付け
行33: A列「スクショ（アカウント）」 B列にフォルダIDを貼り付け
行34: A列「LINE_CSV」             B列にフォルダIDを貼り付け
行35: A列「議事録」                B列にフォルダIDを貼り付け
```

2. メニュー「⚙️ 設定」→「📁 フォルダ設定を反映」を実行
3. 「○件反映しました」と表示されれば完了

※ この設定は一度行えばスプシに保存されます。フォルダを変更したい場合は
   B列を書き換えて再度「📁 フォルダ設定を反映」を実行してください。
